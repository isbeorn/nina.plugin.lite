<ResourceDictionary
    x:Class="WhenPlugin.When.WhenTemplates"
    xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
    xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
    xmlns:cont="clr-namespace:NINA.Sequencer.Container;assembly=NINA.Sequencer"
    xmlns:filter="clr-namespace:NINA.Core.Model;assembly=NINA.Core"
    xmlns:local="clr-namespace:WhenPlugin.When"
    xmlns:logic="clr-namespace:NINA.Sequencer.Logic;assembly=NINA.Sequencer"
    xmlns:mini="clr-namespace:NINA.View.Sequencer.MiniSequencer;assembly=NINA.Sequencer"
    xmlns:nina="clr-namespace:NINA.View.Sequencer;assembly=NINA.Sequencer"
    xmlns:ninactrl="clr-namespace:NINA.CustomControlLibrary;assembly=NINA.CustomControlLibrary"
    xmlns:ns="clr-namespace:NINA.Core.Locale;assembly=NINA.Core"
    xmlns:rules="clr-namespace:NINA.Core.Utility.ValidationRules;assembly=NINA.Core"
    xmlns:util="clr-namespace:NINA.Core.Utility;assembly=NINA.Core"
    xmlns:behaviors="clr-namespace:NINA.Sequencer.Behaviors;assembly=NINA.Sequencer"
    xmlns:wpfutil="clr-namespace:NINA.WPF.Base.Utility;assembly=NINA.WPF.Base"
    xmlns:s="clr-namespace:System;assembly=mscorlib"
    xmlns:i="http://schemas.microsoft.com/xaml/behaviors">

    <!-- Reusable style for collapsible instruction sections -->
    <!-- Usage: Content = instructions container (with Items and DropIntoCommand), Tag = header text -->
    <Style x:Key="CollapsibleInstructionsStyle" TargetType="ContentControl">
        <Setter Property="Template">
            <Setter.Value>
                <ControlTemplate TargetType="ContentControl">
                    <ninactrl:DetachingExpander IsExpanded="False">
                        <ninactrl:DetachingExpander.Header>
                            <TextBlock Text="{TemplateBinding Tag}" Foreground="{StaticResource ButtonForegroundBrush}" VerticalAlignment="Center" Padding="0,8" />
                        </ninactrl:DetachingExpander.Header>
                        <Border MinHeight="35" BorderBrush="{StaticResource BorderBrush}" BorderThickness="1">
                            <Grid Background="{StaticResource BackgroundBrush}" DataContext="{TemplateBinding Content}">
                                <ItemsControl MinHeight="35" ItemsSource="{Binding Items}" />
                                <TextBlock
                                    HorizontalAlignment="Center" VerticalAlignment="Center"
                                    FontStyle="Italic" Opacity="0.4"
                                    Text="{Binding RelativeSource={RelativeSource TemplatedParent}, Path=(local:CollapsibleInstructionsHelper.PlaceholderText)}"
                                    Visibility="{Binding Items.Count, Converter={StaticResource InverseZeroToVisibilityConverter}}" />
                                <i:Interaction.Behaviors>
                                    <behaviors:DragOverBehavior DragAboveSize="0" DragBelowSize="0" />
                                    <behaviors:DropIntoBehavior AllowedDragDropTypesString="NINA.Sequencer.Container.ISequenceContainer;NINA.Sequencer.SequenceItem.ISequenceItem;NINA.Sequencer.TemplatedSequenceContainer" OnDropCommand="DropIntoCommand" />
                                </i:Interaction.Behaviors>
                            </Grid>
                        </Border>
                    </ninactrl:DetachingExpander>
                </ControlTemplate>
            </Setter.Value>
        </Setter>
    </Style>

    <!-- Reusable style for expandable container branches -->
    <!-- Usage: Content = container with Items/DropIntoCommand, Tag = header text -->
    <!-- Set local:CollapsibleInstructionsHelper.PlaceholderText for empty state text -->
    <Style x:Key="ExpandableBranchStyle" TargetType="ContentControl">
        <Setter Property="Template">
            <Setter.Value>
                <ControlTemplate TargetType="ContentControl">
                    <ninactrl:DetachingExpander 
                        IsExpanded="{Binding IsExpanded, Mode=TwoWay}"
                        Margin="0,5,0,0">
                        <ninactrl:DetachingExpander.Header>
                            <Grid Background="{StaticResource TertiaryBackgroundBrush}" MinHeight="30">
                                <StackPanel Orientation="Horizontal">
                                    <TextBlock 
                                        Margin="10,0,0,0"
                                        VerticalAlignment="Center"
                                        Foreground="{StaticResource PrimaryBrush}"
                                        FontWeight="SemiBold"
                                        Text="{TemplateBinding Tag}" />
                                    <TextBlock 
                                        Margin="10,0,0,0"
                                        VerticalAlignment="Center"
                                        Foreground="{StaticResource ButtonForegroundBrush}"
                                        FontStyle="Italic"
                                        Opacity="0.6"
                                        Text="{Binding Items.Count, StringFormat='({0} items)'}" />
                                </StackPanel>
                            </Grid>
                        </ninactrl:DetachingExpander.Header>
                        <Border MinHeight="35" BorderBrush="{StaticResource BorderBrush}" BorderThickness="1">
                            <Grid Background="{StaticResource BackgroundBrush}">
                                <!-- ListBox with virtualization for performance with many items -->
                                <ListBox 
                                    MinHeight="35" 
                                    ItemsSource="{Binding Items}"
                                    VirtualizingPanel.IsVirtualizing="True"
                                    VirtualizingPanel.VirtualizationMode="Recycling"
                                    VirtualizingPanel.ScrollUnit="Pixel"
                                    ScrollViewer.HorizontalScrollBarVisibility="Disabled"
                                    ScrollViewer.VerticalScrollBarVisibility="Auto"
                                    Background="Transparent"
                                    BorderThickness="0"
                                    HorizontalContentAlignment="Stretch">
                                    <ListBox.ItemContainerStyle>
                                        <Style TargetType="ListBoxItem">
                                            <Setter Property="Padding" Value="0"/>
                                            <Setter Property="Margin" Value="0"/>
                                            <Setter Property="HorizontalContentAlignment" Value="Stretch"/>
                                            <Setter Property="Template">
                                                <Setter.Value>
                                                    <ControlTemplate TargetType="ListBoxItem">
                                                        <ContentPresenter />
                                                    </ControlTemplate>
                                                </Setter.Value>
                                            </Setter>
                                        </Style>
                                    </ListBox.ItemContainerStyle>
                                </ListBox>
                                <TextBlock
                                    HorizontalAlignment="Center"
                                    VerticalAlignment="Center"
                                    FontStyle="Italic"
                                    Opacity="0.4"
                                    Text="{Binding RelativeSource={RelativeSource TemplatedParent}, Path=(local:CollapsibleInstructionsHelper.PlaceholderText)}"
                                    Visibility="{Binding Items.Count, Converter={StaticResource InverseZeroToVisibilityConverter}}" />
                                <i:Interaction.Behaviors>
                                    <behaviors:DragOverBehavior DragAboveSize="0" DragBelowSize="0" />
                                    <behaviors:DropIntoBehavior 
                                        AllowedDragDropTypesString="NINA.Sequencer.Container.ISequenceContainer;NINA.Sequencer.SequenceItem.ISequenceItem;NINA.Sequencer.TemplatedSequenceContainer" 
                                        OnDropCommand="DropIntoCommand" />
                                </i:Interaction.Behaviors>
                            </Grid>
                        </Border>
                    </ninactrl:DetachingExpander>
                </ControlTemplate>
            </Setter.Value>
        </Setter>
    </Style>
    
    <ControlTemplate x:Key="PowerupsButtons">
        <StackPanel HorizontalAlignment="Right" Orientation="Horizontal">

            <Button
                   Width="25"
                   Height="25"
                   Margin="25,0,0,0"
                   HorizontalAlignment="Right"
                   VerticalAlignment="Center"
                   Command="{Binding DisableEnableCommand}"
                   Style="{StaticResource SecondaryBackgroundButton}"
                   Visibility="{Binding DetachCommand, Converter={StaticResource NullToVisibilityCollapsedConverter}}">
                <Grid>
                    <Path
                           Margin="5"
                           Data="{StaticResource PowerSVG}"
                           Fill="{StaticResource ButtonForegroundBrush}"
                           Stretch="Uniform"
                           UseLayoutRounding="True" />
                </Grid>
                <Button.ToolTip>
                    <ToolTip>
                        <TextBlock Foreground="{StaticResource PrimaryBrush}" Text="{ns:Loc Lbl_SequenceContainer_Reset_Tooltip}" />
                    </ToolTip>
                </Button.ToolTip>
            </Button>

            <Button
                   Width="25"
                   Height="25"
                   Margin="25,0,0,0"
                   HorizontalAlignment="Right"
                   VerticalAlignment="Center"
                   Command="{Binding ResetProgressCommand}"
                   Style="{StaticResource SecondaryBackgroundButton}"
                   Visibility="{Binding DetachCommand, Converter={StaticResource NullToVisibilityCollapsedConverter}}">
                <Grid>
                    <Path
                           Margin="5"
                           Data="{StaticResource RefreshSVG}"
                           Fill="{StaticResource ButtonForegroundBrush}"
                           Stretch="Uniform"
                           UseLayoutRounding="True" />
                </Grid>
                <Button.ToolTip>
                    <ToolTip>
                        <TextBlock Foreground="{StaticResource PrimaryBrush}" Text="{ns:Loc Lbl_SequenceContainer_Reset_Tooltip}" />
                    </ToolTip>
                </Button.ToolTip>
            </Button>

            <Button
                    Width="25"
                    Height="25"
                    Margin="25,0,0,0"
                    HorizontalAlignment="Right"
                    VerticalAlignment="Center"
                    Command="{Binding DetachCommand}"
                    Style="{StaticResource SecondaryBackgroundButton}"
                    Visibility="{Binding DetachCommand, Converter={StaticResource NullToVisibilityCollapsedConverter}}">
                <Grid>
                    <Path
                            Margin="5"
                            Data="{StaticResource TrashCanSVG}"
                            Fill="{StaticResource ButtonForegroundBrush}"
                            Stretch="Uniform"
                            UseLayoutRounding="True" />
                </Grid>
                <Button.ToolTip>
                    <ToolTip>
                        <TextBlock Foreground="{StaticResource PrimaryBrush}" Text="{ns:Loc Lbl_SequenceContainer_Delete_Tooltip}" />
                    </ToolTip>
                </Button.ToolTip>
            </Button>

            <Button
                    x:Name="CloneContainerButton"
                    Width="25"
                    Height="25"
                    Margin="25,0,0,0"
                    HorizontalAlignment="Center"
                    VerticalAlignment="Center"
                    Command="{Binding AddCloneToParentCommand}"
                    Style="{StaticResource SecondaryBackgroundButton}"
                    Visibility="{Binding AddCloneToParentCommand, Converter={StaticResource NullToVisibilityCollapsedConverter}}">
                <Grid>
                    <Path
                            Margin="5"
                            Data="{StaticResource CopySVG}"
                            Fill="{StaticResource ButtonForegroundBrush}"
                            Stretch="Uniform"
                            UseLayoutRounding="True" />
                </Grid>
                <Button.ToolTip>
                    <ToolTip>
                        <TextBlock Foreground="{StaticResource PrimaryBrush}" Text="{ns:Loc Lbl_SequenceContainer_Duplicate_Tooltip}" />
                    </ToolTip>
                </Button.ToolTip>
            </Button>

            <Button
                    x:Name="MoveUpButton"
                    Width="25"
                    Height="25"
                    Margin="25,0,0,0"
                    HorizontalAlignment="Center"
                    VerticalAlignment="Center"
                    Command="{Binding MoveUpCommand}"
                    Style="{StaticResource SecondaryBackgroundButton}">
                <Grid>
                    <Path
                            Margin="5"
                            Data="{StaticResource ArrowUpSVG}"
                            Fill="{StaticResource ButtonForegroundBrush}"
                            Stretch="Uniform"
                            UseLayoutRounding="True" />
                </Grid>
                <Button.ToolTip>
                    <ToolTip>
                        <TextBlock Foreground="{StaticResource PrimaryBrush}" Text="{ns:Loc Lbl_SequenceContainer_MoveUp_Tooltip}" />
                    </ToolTip>
                </Button.ToolTip>
            </Button>
            <Button
                    x:Name="MoveDownButton"
                    Width="25"
                    Height="25"
                    Margin="10,0,5,0"
                    HorizontalAlignment="Center"
                    VerticalAlignment="Center"
                    Command="{Binding MoveDownCommand}"
                    Style="{StaticResource SecondaryBackgroundButton}"
                    Visibility="{Binding MoveDownCommand, Converter={StaticResource NullToVisibilityCollapsedConverter}}">
                <Grid>
                    <Path
                            Margin="5"
                            Data="{StaticResource ArrowDownSVG}"
                            Fill="{StaticResource ButtonForegroundBrush}"
                            Stretch="Uniform"
                            UseLayoutRounding="True" />
                </Grid>
                <Button.ToolTip>
                    <ToolTip>
                        <TextBlock Foreground="{StaticResource PrimaryBrush}" Text="{ns:Loc Lbl_SequenceContainer_MoveDown_Tooltip}" />
                    </ToolTip>
                </Button.ToolTip>
            </Button>
        </StackPanel>
    </ControlTemplate>

    <Style x:Name="PowerupsStyle" TargetType="{x:Type ninactrl:DetachingExpander}">
        <Setter Property="Template">
            <Setter.Value>
                <ControlTemplate TargetType="{x:Type ninactrl:DetachingExpander}">
                    <Border
                        Margin="0,5,0,5"
                        Background="{TemplateBinding Background}"
                        BorderBrush="{TemplateBinding BorderBrush}"
                        BorderThickness="{TemplateBinding BorderThickness}"
                        Focusable="False"
                        UseLayoutRounding="True">
                        <DockPanel>
                            <ToggleButton
                                x:Name="HeaderSite"
                                MinWidth="0"
                                MinHeight="0"
                                Padding="{TemplateBinding Padding}"
                                HorizontalContentAlignment="{TemplateBinding HorizontalContentAlignment}"
                                VerticalContentAlignment="{TemplateBinding VerticalContentAlignment}"
                                Content="{TemplateBinding Header}"
                                ContentTemplate="{TemplateBinding HeaderTemplate}"
                                ContentTemplateSelector="{TemplateBinding HeaderTemplateSelector}"
                                DockPanel.Dock="Top"
                                FocusVisualStyle="{StaticResource ExpanderHeaderFocusVisual}"
                                FontFamily="{TemplateBinding FontFamily}"
                                FontSize="{TemplateBinding FontSize}"
                                FontStretch="{TemplateBinding FontStretch}"
                                FontStyle="{TemplateBinding FontStyle}"
                                FontWeight="{TemplateBinding FontWeight}"
                                Foreground="{TemplateBinding Foreground}"
                                IsChecked="{Binding IsExpanded, Mode=TwoWay, RelativeSource={RelativeSource TemplatedParent}}">
                                <ToggleButton.Style>
                                    <Style TargetType="ToggleButton">
                                        <Setter Property="Template">
                                            <Setter.Value>
                                                <ControlTemplate TargetType="{x:Type ToggleButton}">
                                                    <StackPanel>
                                                        <Border
                                                            x:Name="topBorder"
                                                            BorderBrush="{StaticResource SecondaryBackgroundBrush}"
                                                            BorderThickness="1,1,0,0">
                                                            <Grid
                                                                 x:Name="Head"
                                                                 Background="{StaticResource SecondaryBackgroundBrush}"
                                                                 UseLayoutRounding="True">
                                                                <Grid.ColumnDefinitions>
                                                                    <ColumnDefinition Width="30" />
                                                                    <ColumnDefinition Width="30" />
                                                                    <ColumnDefinition Width="*" />
                                                                </Grid.ColumnDefinitions>
                                                                <Path
                                                                    x:Name="iconarrow"
                                                                    Grid.Column="0"
                                                                    Width="10"
                                                                    Height="10"
                                                                    Margin="10,0,10,0"
                                                                    Data="{StaticResource ArrowRightSVG}"
                                                                    Fill="{StaticResource ButtonForegroundBrush}"
                                                                    Stretch="Uniform"
                                                                    UseLayoutRounding="True" />
                                                                <Path
                                                                    x:Name="icon"
                                                                    Grid.Column="1"
                                                                    Margin="5,0,3,0"
                                                                    Data="{StaticResource BoxClosedSVG}"
                                                                    Fill="{StaticResource ButtonForegroundBrush}"
                                                                    Stretch="Uniform"
                                                                    UseLayoutRounding="True" />
                                                                <ContentPresenter
                                                                    Grid.Column="2"
                                                                    Margin="4,0,0,0"
                                                                    HorizontalAlignment="Left"
                                                                    VerticalAlignment="Center"
                                                                    RecognizesAccessKey="True"
                                                                    UseLayoutRounding="True">
                                                                    <i:Interaction.Behaviors>
                                                                        <!--  This gives the ability to drag and drop a sequence container by its header  -->
                                                                        <behaviors:DragDropBehavior />
                                                                    </i:Interaction.Behaviors>
                                                                </ContentPresenter>
                                                            </Grid>
                                                        </Border>
                                                    </StackPanel>
                                                    <ControlTemplate.Triggers>
                                                        <Trigger Property="IsChecked" Value="true">
                                                            <Setter TargetName="icon" Property="Data" Value="{StaticResource BoxSVG}" />
                                                            <Setter TargetName="iconarrow" Property="Data" Value="{StaticResource ArrowDownSVG}" />
                                                            <Setter TargetName="iconarrow" Property="Margin" Value="5,0,10,0" />
                                                        </Trigger>
                                                        <Trigger Property="IsChecked" Value="false">
                                                            <Setter TargetName="topBorder" Property="BorderThickness" Value="1,1,1,1" />
                                                        </Trigger>
                                                        <Trigger Property="IsMouseOver" Value="True">
                                                            <Setter TargetName="Head" Property="Background" Value="{StaticResource ButtonBackgroundSelectedBrush}" />
                                                        </Trigger>
                                                    </ControlTemplate.Triggers>
                                                </ControlTemplate>
                                            </Setter.Value>
                                        </Setter>
                                    </Style>
                                </ToggleButton.Style>
                            </ToggleButton>
                            <Border x:Name="ContainerBorder" BorderBrush="{StaticResource SecondaryBackgroundBrush}">
                                <ContentPresenter
                                                    x:Name="ExpandSite"
                                                    Margin="{TemplateBinding Padding}"
                                                    HorizontalAlignment="{TemplateBinding HorizontalContentAlignment}"
                                                    VerticalAlignment="{TemplateBinding VerticalContentAlignment}"
                                                    Focusable="false" />
                            </Border>
                       </DockPanel>
                    </Border>
                    <ControlTemplate.Triggers>
                        <Trigger Property="IsExpanded" Value="true">
                            <Setter TargetName="ExpandSite" Property="Visibility" Value="Visible" />
                            <Setter TargetName="ContainerBorder" Property="BorderThickness" Value="10,0,0,15" />
                        </Trigger>
                        <Trigger Property="IsExpanded" Value="False">
                            <Setter TargetName="ExpandSite" Property="Visibility" Value="Collapsed" />
                            <Setter TargetName="ContainerBorder" Property="BorderThickness" Value="0" />
                        </Trigger>
                        <Trigger Property="IsEnabled" Value="false">
                            <Setter Property="Foreground" Value="{DynamicResource {x:Static SystemColors.GrayTextBrushKey}}" />
                        </Trigger>
                    </ControlTemplate.Triggers>
                </ControlTemplate>
            </Setter.Value>
        </Setter>
    </Style>

  <!-- GSSend (Send via Ground Station) -->
  <DataTemplate DataType="{x:Type local:GSSend}">
    <nina:SequenceBlockView DataContext="{Binding}">
      <nina:SequenceBlockView.SequenceItemContent>
        <StackPanel>
          <!-- Condition drop slot (expects a single GS sequence item) -->
          <Border
              MinHeight="35"
              BorderBrush="{StaticResource BorderBrush}"
              BorderThickness="1"
              Background="{StaticResource BackgroundBrush}">
            <Grid DataContext="{Binding}">
              <ContentPresenter Content="{Binding Condition.Items[0]}" />
              <TextBlock
                  HorizontalAlignment="Center"
                  VerticalAlignment="Center"
                  FontStyle="Italic"
                  Opacity="0.4"
                  Text="Drop Ground Station instruction here"
                  Visibility="{Binding Condition.Items.Count, Converter={StaticResource InverseZeroToVisibilityConverter}}" />
              <i:Interaction.Behaviors>
                <behaviors:DragOverBehavior DragAboveSize="0" DragBelowSize="0" />
                <behaviors:DropIntoBehavior
                    AllowedDragDropTypesString="NINA.Sequencer.SequenceItem.ISequenceItem"
                    OnDropCommand="DropIntoIfCommand" />
              </i:Interaction.Behaviors>
            </Grid>
          </Border>

          <!-- Optional additional instructions container -->
          <ContentControl
              Style="{StaticResource CollapsibleInstructionsStyle}"
              Content="{Binding Instructions}"
              Tag="Instructions"
              local:CollapsibleInstructionsHelper.PlaceholderText="Additional instructions (optional)" />
        </StackPanel>
      </nina:SequenceBlockView.SequenceItemContent>
    </nina:SequenceBlockView>
  </DataTemplate>

  <!-- WhenSwitch (Trigger) - compact layout + predicate ExprControl + droppable Instructions -->
  <DataTemplate DataType="{x:Type local:WhenSwitch}">
    <Border>
      <Border.Resources>
        <wpfutil:SharedResourceDictionary Source="/NINA.Sequencer;component/Resources/Styles/ProgressStyle.xaml" />
      </Border.Resources>

      <i:Interaction.Behaviors>
        <behaviors:DragOverBehavior AllowDragCenter="False" DragAboveSize="25" DragBelowSize="15" />
      </i:Interaction.Behaviors>

      <ninactrl:DetachingExpander IsExpanded="{Binding IsExpanded, Mode=TwoWay}">
        <ninactrl:DetachingExpander.Header>
          <Grid
              MinHeight="45"
              Margin="0"
              HorizontalAlignment="{Binding HorizontalAlignment, RelativeSource={RelativeSource AncestorType=ContentPresenter}, Mode=OneWayToSource}"
              Background="{StaticResource SecondaryBackgroundBrush}">
            <Grid.ColumnDefinitions>
              <ColumnDefinition Width="Auto" />
              <ColumnDefinition />
              <ColumnDefinition Width="Auto" />
            </Grid.ColumnDefinitions>

            <StackPanel Orientation="Horizontal">
              <TextBlock
                  MinHeight="25"
                  Margin="10,10,40,0"
                  Foreground="White"
                  HorizontalAlignment="Left"
                  VerticalAlignment="Center"
                  Text="{Binding Name}" />

              <Border
                  Width="20"
                  Height="20"
                  Margin="5,0,5,0"
                  Background="{StaticResource NotificationErrorBrush}"
                  BorderBrush="Transparent"
                  CornerRadius="10">
                <Border.Visibility>
                  <PriorityBinding>
                    <Binding Path="Issues.Count" Converter="{StaticResource ZeroToVisibilityConverter}" FallbackValue="Collapsed" />
                  </PriorityBinding>
                </Border.Visibility>
                <Path
                    HorizontalAlignment="Right"
                    Data="{StaticResource ExclamationCircledSVG}"
                    Fill="{StaticResource ButtonForegroundBrush}"
                    Stretch="Uniform" />
                <Border.ToolTip>
                  <ItemsControl ItemsSource="{Binding Issues}" />
                </Border.ToolTip>
              </Border>

              <!-- Predicate expression editor -->
              <logic:ExprControl Label="Predicate:" Exp="{Binding PredicateExpression}" />
            </StackPanel>

            <Border Grid.Column="1" Background="{StaticResource SecondaryBackgroundBrush}">
              <ContentPresenter
                  Margin="2,0,10,0"
                  HorizontalAlignment="Right"
                  Content="{Binding}"
                  DockPanel.Dock="Right"
                  Style="{StaticResource ProgressPresenter}" />
            </Border>

            <Border
                x:Name="ButtonCommands"
                Grid.Column="2"
                HorizontalAlignment="Stretch"
                Background="{StaticResource SecondaryBackgroundBrush}">
              <Control Template="{StaticResource PowerupsButtons}" />
            </Border>
          </Grid>
        </ninactrl:DetachingExpander.Header>

        <!-- Single compact droppable instructions area -->
        <Border
            MinHeight="35"
            Margin="0,0,0,0"
            BorderBrush="{StaticResource BorderBrush}"
            BorderThickness="1">
          <Grid Background="{StaticResource BackgroundBrush}" DataContext="{Binding Instructions}">
            <ItemsControl MinHeight="35" ItemsSource="{Binding Items}" />
            <TextBlock
                HorizontalAlignment="Left"
                VerticalAlignment="Center"
                Margin="30,0,0,0"
                FontStyle="Italic"
                Opacity="0.4"
                Text="Instructions"
                Visibility="{Binding Items.Count, Converter={StaticResource InverseZeroToVisibilityConverter}}" />

            <i:Interaction.Behaviors>
              <behaviors:DragOverBehavior DragAboveSize="0" DragBelowSize="0" />
              <behaviors:DropIntoBehavior
                  AllowedDragDropTypesString="NINA.Sequencer.Container.ISequenceContainer;NINA.Sequencer.SequenceItem.ISequenceItem;NINA.Sequencer.TemplatedSequenceContainer"
                  OnDropCommand="DropIntoCommand" />
            </i:Interaction.Behaviors>
          </Grid>
        </Border>
      </ninactrl:DetachingExpander>
    </Border>
  </DataTemplate>
  
  <!-- WhenUnsafe (Trigger) - compact droppable Instructions like WhenSwitch -->
  <DataTemplate DataType="{x:Type local:WhenUnsafe}">
    <Border>
      <Border.Resources>
        <wpfutil:SharedResourceDictionary Source="/NINA.Sequencer;component/Resources/Styles/ProgressStyle.xaml" />
      </Border.Resources>

      <i:Interaction.Behaviors>
        <behaviors:DragOverBehavior AllowDragCenter="False" DragAboveSize="25" DragBelowSize="15" />
      </i:Interaction.Behaviors>

      <ninactrl:DetachingExpander IsExpanded="{Binding IsExpanded, Mode=TwoWay}">
        <ninactrl:DetachingExpander.Header>
          <Grid
              MinHeight="45"
              Margin="0"
              HorizontalAlignment="{Binding HorizontalAlignment, RelativeSource={RelativeSource AncestorType=ContentPresenter}, Mode=OneWayToSource}"
              Background="{StaticResource SecondaryBackgroundBrush}">
            <Grid.ColumnDefinitions>
              <ColumnDefinition Width="Auto" />
              <ColumnDefinition />
              <ColumnDefinition Width="Auto" />
            </Grid.ColumnDefinitions>

            <StackPanel Orientation="Horizontal">
              <TextBlock
                  MinHeight="25"
                  Margin="10,10,40,0"
                  Foreground="White"
                  HorizontalAlignment="Left"
                  VerticalAlignment="Center"
                  Text="{Binding Name}" />

              <Border
                  Width="20"
                  Height="20"
                  Margin="5,0,5,0"
                  Background="{StaticResource NotificationErrorBrush}"
                  BorderBrush="Transparent"
                  CornerRadius="10">
                <Border.Visibility>
                  <PriorityBinding>
                    <Binding Path="Issues.Count" Converter="{StaticResource ZeroToVisibilityConverter}" FallbackValue="Collapsed" />
                  </PriorityBinding>
                </Border.Visibility>
                <Path
                    HorizontalAlignment="Right"
                    Data="{StaticResource ExclamationCircledSVG}"
                    Fill="{StaticResource ButtonForegroundBrush}"
                    Stretch="Uniform" />
                <Border.ToolTip>
                  <ItemsControl ItemsSource="{Binding Issues}" />
                </Border.ToolTip>
              </Border>
            </StackPanel>

            <Border Grid.Column="1" Background="{StaticResource SecondaryBackgroundBrush}">
              <ContentPresenter
                  Margin="2,0,10,0"
                  HorizontalAlignment="Right"
                  Content="{Binding}"
                  DockPanel.Dock="Right"
                  Style="{StaticResource ProgressPresenter}" />
            </Border>

            <Border
                x:Name="ButtonCommands"
                Grid.Column="2"
                HorizontalAlignment="Stretch"
                Background="{StaticResource SecondaryBackgroundBrush}">
              <Control Template="{StaticResource PowerupsButtons}" />
            </Border>
          </Grid>
        </ninactrl:DetachingExpander.Header>

        <!-- Single compact droppable instructions area -->
        <Border
            MinHeight="35"
            Margin="0,0,0,0"
            BorderBrush="{StaticResource BorderBrush}"
            BorderThickness="1">
          <Grid Background="{StaticResource BackgroundBrush}" DataContext="{Binding Instructions}">
            <ItemsControl MinHeight="35" ItemsSource="{Binding Items}" />
            <TextBlock
                HorizontalAlignment="Left"
                VerticalAlignment="Center"
                Margin="30,0,0,0"
                FontStyle="Italic"
                Opacity="0.4"
                Text="Instructions"
                Visibility="{Binding Items.Count, Converter={StaticResource InverseZeroToVisibilityConverter}}" />
            <i:Interaction.Behaviors>
              <behaviors:DragOverBehavior DragAboveSize="0" DragBelowSize="0" />
              <behaviors:DropIntoBehavior
                  AllowedDragDropTypesString="NINA.Sequencer.Container.ISequenceContainer;NINA.Sequencer.SequenceItem.ISequenceItem;NINA.Sequencer.TemplatedSequenceContainer"
                  OnDropCommand="DropIntoCommand" />
            </i:Interaction.Behaviors>
          </Grid>
        </Border>
      </ninactrl:DetachingExpander>
    </Border>
  </DataTemplate>
  
  <HierarchicalDataTemplate DataType="{x:Type local:TemplateByReference}" ItemsSource="{Binding Items}">
        <local:PowerupsContainerView AllowConditions="False" AllowTriggers="False" AllowInstructions="False">
            <local:PowerupsContainerView.HeaderAddon>
                <StackPanel Orientation="Horizontal">
                    <TextBlock
                        Foreground="{StaticResource ButtonForegroundBrush}"
                        Margin="20,0,0,0"
                        HorizontalAlignment="Left"
                        VerticalAlignment="Center"
                        Text="Template Name:" />
                    <ComboBox
                        Foreground="{StaticResource ButtonForegroundBrush}"
                        Margin="10,0,0,0"
                        DisplayMemberPath="Container.Name"
                        ItemsSource="{Binding SortedTemplates}"
                        SelectedItem="{Binding SelectedTemplate, Mode=TwoWay, UpdateSourceTrigger=PropertyChanged}"
                        SelectedValuePath="Container.Name" />
                </StackPanel>
            </local:PowerupsContainerView.HeaderAddon>
        </local:PowerupsContainerView>
    </HierarchicalDataTemplate>

  <HierarchicalDataTemplate DataType="{x:Type local:IfTimeout}" ItemsSource="{Binding Items}">
    <local:PowerupsContainerView>
      <local:PowerupsContainerView.HeaderAddon>
        <Border
           MinHeight="30"
           Margin="10,1,0,0"
           BorderBrush="{StaticResource BorderBrush}"
           BorderThickness="1">
          <Grid Background="{StaticResource BackgroundBrush}" DataContext="{Binding}">
            <StackPanel Visibility="{Binding CheckInstruction, Converter={StaticResource NullToVisibilityCollapsedConverter}}">
              <ContentPresenter Content="{Binding CheckInstruction}" />
            </StackPanel>
            <TextBlock
               HorizontalAlignment="Center"
               VerticalAlignment="Center"
               FontStyle="Italic"
               Text="Instruction to check goes here"
               TextAlignment="Center"
               MinWidth="400"
               Visibility="{Binding CheckInstruction, Converter={StaticResource InverseNullToVisibilityConverter}}" />
            <i:Interaction.Behaviors>
              <behaviors:DragOverBehavior DragAboveSize="0" DragBelowSize="0" />
            </i:Interaction.Behaviors>
          </Grid>
          <i:Interaction.Behaviors>
            <behaviors:DropIntoBehavior AllowedDragDropTypesString="NINA.Sequencer.SequenceItem.ISequenceItem" OnDropCommand="DropIntoIfCommand" />
          </i:Interaction.Behaviors>
        </Border>
      </local:PowerupsContainerView.HeaderAddon>
    </local:PowerupsContainerView>
  </HierarchicalDataTemplate>

  <HierarchicalDataTemplate DataType="{x:Type local:IfFailed}" ItemsSource="{Binding Items}">
    <local:PowerupsContainerView>
      <local:PowerupsContainerView.HeaderAddon>
        <Border
           MinHeight="30"
           Margin="10,1,0,0"
           BorderBrush="{StaticResource BorderBrush}"
           BorderThickness="1">
          <Grid Background="{StaticResource BackgroundBrush}" DataContext="{Binding}">
            <StackPanel Visibility="{Binding CheckInstruction, Converter={StaticResource NullToVisibilityCollapsedConverter}}">
              <ContentPresenter Content="{Binding CheckInstruction}" />
            </StackPanel>
            <TextBlock
               HorizontalAlignment="Center"
               VerticalAlignment="Center"
               FontStyle="Italic"
               Text="Instruction to check goes here"
               TextAlignment="Center"
               MinWidth="400"
               Visibility="{Binding CheckInstruction, Converter={StaticResource InverseNullToVisibilityConverter}}" />
            <i:Interaction.Behaviors>
              <behaviors:DragOverBehavior DragAboveSize="0" DragBelowSize="0" />
            </i:Interaction.Behaviors>
          </Grid>
          <i:Interaction.Behaviors>
            <behaviors:DropIntoBehavior AllowedDragDropTypesString="NINA.Sequencer.SequenceItem.ISequenceItem" OnDropCommand="DropIntoIfCommand" />
          </i:Interaction.Behaviors>
        </Border>
      </local:PowerupsContainerView.HeaderAddon>
    </local:PowerupsContainerView>
  </HierarchicalDataTemplate>
  
    <!-- IfThenElse DataTemplate using reusable branch style -->
    <DataTemplate DataType="{x:Type local:IfThenElse}">
        <Border>
            <Border.Resources>
                <wpfutil:SharedResourceDictionary Source="/NINA.Sequencer;component/Resources/Styles/ProgressStyle.xaml" />
            </Border.Resources>
            <i:Interaction.Behaviors>
                <behaviors:DragOverBehavior AllowDragCenter="False" DragAboveSize="25" DragBelowSize="15" />
            </i:Interaction.Behaviors>
            <ninactrl:DetachingExpander IsExpanded="{Binding IsExpanded, Mode=TwoWay}">
                <ninactrl:DetachingExpander.Header>
                    <Grid
                     MinHeight="45"
                     Margin="0"
                     HorizontalAlignment="{Binding HorizontalAlignment, RelativeSource={RelativeSource AncestorType=ContentPresenter}, Mode=OneWayToSource}"
                     Background="{StaticResource SecondaryBackgroundBrush}">
                        <Grid.ColumnDefinitions>
                            <ColumnDefinition Width="Auto" />
                            <ColumnDefinition />
                            <ColumnDefinition Width="Auto" />
                        </Grid.ColumnDefinitions>
                        <StackPanel Orientation="Horizontal">
                            <TextBlock 
                                MinHeight="25"
                                Margin="10,10,40,0"
                                Foreground="White"
                                HorizontalAlignment="Left"
                                VerticalAlignment="Center"
                                Text="{Binding Name}" />
                            <Border
                              Width="20"
                              Height="20"
                              Margin="5,0,5,0"
                              Background="{StaticResource NotificationErrorBrush}"
                              BorderBrush="Transparent"
                              CornerRadius="10">
                                <Border.Visibility>
                                    <PriorityBinding>
                                        <Binding
                                          Converter="{StaticResource ZeroToVisibilityConverter}"
                                          FallbackValue="Collapsed"
                                          Path="Issues.Count" />
                                    </PriorityBinding>
                                </Border.Visibility>
                                <Path
                                  HorizontalAlignment="Right"
                                  Data="{StaticResource ExclamationCircledSVG}"
                                  Fill="{StaticResource ButtonForegroundBrush}"
                                  Stretch="Uniform" />
                                <Border.ToolTip>
                                    <ItemsControl ItemsSource="{Binding Issues}" />
                                </Border.ToolTip>
                            </Border>
                            <logic:ExprControl Label="Predicate:" Exp="{Binding PredicateExpression}" />
                        </StackPanel>
                        <Border Grid.Column="1" Background="{StaticResource SecondaryBackgroundBrush}">
                            <ContentPresenter
                             Margin="2,0,10,0"
                             HorizontalAlignment="Right"
                             Content="{Binding}"
                             DockPanel.Dock="Right"
                             Style="{StaticResource ProgressPresenter}" />
                        </Border>
                        <Border
                          x:Name="ButtonCommands"
                          Grid.Column="2"
                          HorizontalAlignment="Stretch"
                          Background="{StaticResource SecondaryBackgroundBrush}">
                            <Control Template="{StaticResource PowerupsButtons}" />
                        </Border>
                    </Grid>
                </ninactrl:DetachingExpander.Header>
                <StackPanel Orientation="Vertical" Margin="10,0,0,0">
                    <!-- Then Branch -->
                    <ContentControl Style="{StaticResource ExpandableBranchStyle}" 
                        Content="{Binding ThenBranch}" 
                        DataContext="{Binding ThenBranch}"
                        Tag="Then"
                        local:CollapsibleInstructionsHelper.PlaceholderText="Instructions to run if predicate is true" />
                    <!-- Else Branch -->
                    <ContentControl Style="{StaticResource ExpandableBranchStyle}" 
                        Content="{Binding ElseBranch}" 
                        DataContext="{Binding ElseBranch}"
                        Tag="Else"
                        local:CollapsibleInstructionsHelper.PlaceholderText="Instructions to run if predicate is false" />
                </StackPanel>
            </ninactrl:DetachingExpander>
        </Border>
    </DataTemplate>

    <DataTemplate DataType="{x:Type local:ForEachInArray}">
        <Border>
            <Border.Resources>
                <wpfutil:SharedResourceDictionary Source="/NINA.Sequencer;component/Resources/Styles/ProgressStyle.xaml" />
            </Border.Resources>
            <i:Interaction.Behaviors>
                <!--  Drop area above and below the Sequence Container  -->
                <behaviors:DragOverBehavior
                 AllowDragCenter="False"
                 DragAboveSize="25"
                 DragBelowSize="15" />
            </i:Interaction.Behaviors>
            <ninactrl:DetachingExpander>
                <ninactrl:DetachingExpander.Header>
                    <Grid
                     MinHeight="45"
                     Margin="0"
                     HorizontalAlignment="{Binding HorizontalAlignment, RelativeSource={RelativeSource AncestorType=ContentPresenter}, Mode=OneWayToSource}"
                     Background="{StaticResource SecondaryBackgroundBrush}">
                        <Grid.ColumnDefinitions>
                            <ColumnDefinition Width="Auto" />
                            <ColumnDefinition />
                            <ColumnDefinition Width="Auto" />
                        </Grid.ColumnDefinitions>
                        <StackPanel Orientation="Horizontal">
                            <TextBlock 
                                MinHeight="25"
                                Margin="10,10,10,0"
                                Foreground="White"
                                HorizontalAlignment="Left"
                                VerticalAlignment="Center"
                                Text="For Each in Array" />
                            <Border
                              Width="20"
                              Height="20"
                              Margin="5,0,5,0"
                              Background="{StaticResource NotificationErrorBrush}"
                              BorderBrush="Transparent"
                              CornerRadius="10">
                                <Border.Visibility>
                                    <PriorityBinding>
                                        <Binding
                                          Converter="{StaticResource ZeroToVisibilityConverter}"
                                          FallbackValue="Collapsed"
                                          Path="Issues.Count" />
                                    </PriorityBinding>
                                </Border.Visibility>
                                <Path
                                  HorizontalAlignment="Right"
                                  Data="{StaticResource ExclamationCircledSVG}"
                                  Fill="{StaticResource ButtonForegroundBrush}"
                                  Stretch="Uniform" />
                                <Border.ToolTip>
                                    <ItemsControl ItemsSource="{Binding Issues}" />
                                </Border.ToolTip>
                            </Border>
                            <TextBlock Margin="0,3,0,0" VerticalAlignment="Center" Text="Index Variable:" />
                            <TextBox
                               MinWidth="40"
                               Margin="5,2,10,-2"
                               Text="{Binding IndexVariable}"
                               VerticalAlignment="Center"
                               TextAlignment="Center" />
                            <TextBlock Margin="0,3,0,0" VerticalAlignment="Center" Text="Value Variable:" />
                            <TextBox
                               MinWidth="40"
                               Margin="5,2,10,-2"
                               Text="{Binding ValueVariable}"
                               VerticalAlignment="Center"
                               TextAlignment="Center" />
                            <logic:ExprControl Label="Name:" Exp="{Binding NameExprExpression}" />
                        </StackPanel>
                        <Border Grid.Column="1" Background="{StaticResource SecondaryBackgroundBrush}">
                            <ContentPresenter
                             Margin="2,0,10,0"
                             HorizontalAlignment="Right"
                             Content="{Binding}"
                             DockPanel.Dock="Right"
                             Style="{StaticResource ProgressPresenter}" />
                        </Border>
                        <Border
                          x:Name="ButtonCommands"
                          Grid.Column="2"
                          HorizontalAlignment="Stretch"
                          Background="{StaticResource SecondaryBackgroundBrush}">
                            <Control Template="{StaticResource PowerupsButtons}" />
                        </Border>
                    </Grid>
                </ninactrl:DetachingExpander.Header>
                <StackPanel Grid.Row="1" Orientation="Vertical">
                    <Border
                     MinHeight="35"
                     Margin="0,0,0,0"
                     BorderBrush="{StaticResource BorderBrush}"
                     BorderThickness="1">
                        <Grid Background="{StaticResource BackgroundBrush}" >
                            <ItemsControl MinHeight="35" ItemsSource="{Binding Items}" />
                            <TextBlock
                             HorizontalAlignment="Center"
                             VerticalAlignment="Center"
                             FontStyle="Italic"
                             Opacity="0.4"
                             Text="Conditional instructions go here"
                             Visibility="{Binding Items.Count, Converter={StaticResource InverseZeroToVisibilityConverter}}" />
                            <i:Interaction.Behaviors>
                                <behaviors:DragOverBehavior DragAboveSize="0" DragBelowSize="0" />
                                <behaviors:DropIntoBehavior AllowedDragDropTypesString="NINA.Sequencer.Container.ISequenceContainer;NINA.Sequencer.SequenceItem.ISequenceItem;NINA.Sequencer.TemplatedSequenceContainer" OnDropCommand="DropIntoCommand" />
                            </i:Interaction.Behaviors>
                        </Grid>
                    </Border>
                </StackPanel>
            </ninactrl:DetachingExpander>
        </Border>
    </DataTemplate>

    <HierarchicalDataTemplate DataType="{x:Type local:ForEachList}" ItemsSource="{Binding Items}">
        <local:PowerupsContainerView AllowConditions="False" AllowTriggers="False" AllowInstructions="False">
            <local:PowerupsContainerView.HeaderAddon>
                <StackPanel Orientation="Horizontal">
                    <TextBlock VerticalAlignment="Center" Text="Variable(s):" />
                    <TextBox
                        MinWidth="40"
                        Margin="5,2,10,-2"
                        Text="{Binding Variable}"
                        VerticalAlignment="Center"
                        TextAlignment="Center" />
                    <TextBlock VerticalAlignment="Center" Text="Expression(s):" />
                    <TextBox
                        MinWidth="40"
                        Margin="5,2,10,-2"
                        Text="{Binding ListExpression}"
                        VerticalAlignment="Center"
                        TextAlignment="Center" />
                </StackPanel>
            </local:PowerupsContainerView.HeaderAddon>
        </local:PowerupsContainerView>
    </HierarchicalDataTemplate>


    <DataTemplate DataType="{x:Type local:PutArray}">
        <nina:SequenceBlockView DataContext="{Binding}">
            <nina:SequenceBlockView.SequenceItemContent>
                <StackPanel Orientation="Horizontal">
                    <logic:ExprControl Label="Name:" Exp="{Binding NameExprExpression}" />
                    <logic:ExprControl Margin="8" Label="Index:" Exp="{Binding IExprExpression}" />
                    <logic:ExprControl Label="Value:" Exp="{Binding VExprExpression}"/>
                </StackPanel>
            </nina:SequenceBlockView.SequenceItemContent>
        </nina:SequenceBlockView>
    </DataTemplate>

    <DataTemplate DataType="{x:Type local:GetArray}">
        <nina:SequenceBlockView DataContext="{Binding}">
            <nina:SequenceBlockView.SequenceItemContent>
                <StackPanel Orientation="Horizontal">
                    <logic:ExprControl Label="Name:" Exp="{Binding NameExprExpression}" />
                    <logic:ExprControl Margin="8" Label="Index:" Exp="{Binding IExprExpression}" />
                    <logic:ExprControl Label="Variable:" Exp="{Binding VExprExpression}"/>
                </StackPanel>
            </nina:SequenceBlockView.SequenceItemContent>
        </nina:SequenceBlockView>
    </DataTemplate>

    <DataTemplate DataType="{x:Type local:InitializeArray}">
        <nina:SequenceBlockView DataContext="{Binding}">
            <nina:SequenceBlockView.SequenceItemContent>
                <StackPanel Orientation="Horizontal">
                    <logic:ExprControl Label="Name:" Exp="{Binding NameExprExpression}" />
                </StackPanel>
            </nina:SequenceBlockView.SequenceItemContent>
        </nina:SequenceBlockView>
    </DataTemplate>

    <DataTemplate DataType="{x:Type local:DIYTrigger}">
        <nina:SequenceBlockView DataContext="{Binding}">
            <nina:SequenceBlockView.SequenceItemContent>
                <StackPanel>
                    <Border MinHeight="35" BorderBrush="{StaticResource BorderBrush}" BorderThickness="1" Background="{StaticResource BackgroundBrush}">
                        <StackPanel Orientation="Horizontal">
                            <Border Width="20" Height="20" Margin="10,0,5,0" 
                                Background="{StaticResource NotificationErrorBrush}"
                                BorderBrush="Transparent" CornerRadius="10"
                                Visibility="{Binding Issues.Count, Converter={StaticResource ZeroToVisibilityConverter}, FallbackValue=Collapsed}">
                                <Path Data="{StaticResource ExclamationCircledSVG}" Fill="{StaticResource ButtonForegroundBrush}" Stretch="Uniform" />
                                <Border.ToolTip><ItemsControl ItemsSource="{Binding Issues}" /></Border.ToolTip>
                            </Border>
                            <TextBlock Margin="10,0,0,0" VerticalAlignment="Center" Text="Trigger:" />
                            <ItemsControl MinHeight="35" Margin="10,0,0,0" ItemsSource="{Binding TriggerRunner.Triggers}" />
                            <TextBlock
                                VerticalAlignment="Center"
                                FontStyle="Italic" Opacity="0.4"
                                Text="Drop trigger here"
                                Visibility="{Binding TriggerRunner.Triggers.Count, Converter={StaticResource InverseZeroToVisibilityConverter}}"
                                MinWidth="150"/>
                            <i:Interaction.Behaviors>
                                <behaviors:DragOverBehavior DragAboveSize="0" DragBelowSize="0" />
                                <behaviors:DropIntoBehavior AllowedDragDropTypesString="NINA.Sequencer.Trigger.SequenceTrigger" OnDropCommand="DropIntoDIYTriggersCommand" />
                            </i:Interaction.Behaviors>
                        </StackPanel>
                    </Border>
                    <ContentControl Style="{StaticResource CollapsibleInstructionsStyle}" 
                        Content="{Binding TriggerRunner}" Tag="Instructions"
                        local:CollapsibleInstructionsHelper.PlaceholderText="Instructions to run when trigger fires" />
                </StackPanel>
            </nina:SequenceBlockView.SequenceItemContent>
        </nina:SequenceBlockView>
    </DataTemplate>


    <DataTemplate DataType="{x:Type local:ConditionalTrigger}">
        <nina:SequenceBlockView DataContext="{Binding}">
            <nina:SequenceBlockView.SequenceItemContent>
                <Border MinHeight="35"
                      BorderBrush="{StaticResource BorderBrush}"
                      BorderThickness="1">
                    <StackPanel Orientation="Horizontal" Background="{StaticResource BackgroundBrush}">
                        <logic:ExprControl Label="Enabling Expression:" Exp="{Binding Path=PredicateExpression, Mode=TwoWay}"/>
                        <StackPanel Orientation="Horizontal" Margin="20,0,0,0">
                            <ItemsControl MinHeight="35" ItemsSource="{Binding TriggerRunner.Triggers}"/>
                            <TextBlock
                               HorizontalAlignment="Center"
                               VerticalAlignment="Center"
                               TextAlignment="Left"
                               FontStyle="Italic"
                               Opacity="0.4"
                               Text="Drop the Trigger here"
                               Visibility="{Binding TriggerRunner.Triggers.Count, Converter={StaticResource InverseZeroToVisibilityConverter}}"
                               MinWidth="200"/>
                            <i:Interaction.Behaviors>
                                <behaviors:DragOverBehavior DragAboveSize="0" DragBelowSize="0" />
                                <behaviors:DropIntoBehavior AllowedDragDropTypesString="NINA.Sequencer.Trigger.SequenceTrigger" OnDropCommand="DropIntoDIYTriggersCommand" />
                            </i:Interaction.Behaviors>
                        </StackPanel>
                    </StackPanel>
                </Border>
            </nina:SequenceBlockView.SequenceItemContent>
        </nina:SequenceBlockView>
    </DataTemplate>
    
    <DataTemplate DataType="{x:Type local:SafeTrigger}">
        <nina:SequenceBlockView DataContext="{Binding}">
            <nina:SequenceBlockView.SequenceItemContent>
                <Border MinHeight="35"
                        BorderBrush="{StaticResource BorderBrush}"
                        BorderThickness="1">
                    <Grid Background="{StaticResource BackgroundBrush}" DataContext="{Binding TriggerRunner}">
                        <ItemsControl MinHeight="35" ItemsSource="{Binding Triggers}" />
                        <TextBlock
                            HorizontalAlignment="Center"
                            VerticalAlignment="Center"
                            TextAlignment="Center"
                            FontStyle="Italic"
                            Opacity="0.4"
                            Text="Trigger goes here"
                            Visibility="{Binding Triggers.Count, Converter={StaticResource InverseZeroToVisibilityConverter}}"
                            MinWidth="200"/>
                        <i:Interaction.Behaviors>
                            <behaviors:DragOverBehavior DragAboveSize="0" DragBelowSize="0" />
                        </i:Interaction.Behaviors>
                    </Grid>
                    <i:Interaction.Behaviors>
                        <behaviors:DropIntoBehavior AllowedDragDropTypesString="NINA.Sequencer.Trigger.SequenceTrigger" OnDropCommand="SafeTriggerCommand" />
                    </i:Interaction.Behaviors>
                </Border>
            </nina:SequenceBlockView.SequenceItemContent>
        </nina:SequenceBlockView>
    </DataTemplate>

  
    <DataTemplate DataType="{x:Type local:InterruptTrigger}">
        <nina:SequenceBlockView DataContext="{Binding}">
            <nina:SequenceBlockView.SequenceItemContent>
                <ninactrl:DetachingExpander>
                    <Grid>
                        <Grid.RowDefinitions>
                            <RowDefinition Height="Auto" />
                            <RowDefinition Height="Auto" />
                        </Grid.RowDefinitions>
                        <Border
                        Grid.Row="1"
                        MinHeight="65"
                        BorderBrush="{StaticResource BorderBrush}"
                        BorderThickness="1">
                            <Grid Background="{StaticResource BackgroundBrush}" DataContext="{Binding Runner}">
                                <ItemsControl MinHeight="35" ItemsSource="{Binding Items}" />
                                <TextBlock
                                HorizontalAlignment="Center"
                                VerticalAlignment="Center"
                                FontStyle="Italic"
                                Opacity="0.4"
                                Text="Instructions will go here..."
                                Visibility="{Binding Items.Count, Converter={StaticResource InverseZeroToVisibilityConverter}}" />
                                <i:Interaction.Behaviors>
                                    <behaviors:DragOverBehavior DragAboveSize="0" DragBelowSize="0" />
                                    <behaviors:DropIntoBehavior AllowedDragDropTypesString="NINA.Sequencer.Container.ISequenceContainer;NINA.Sequencer.TemplatedSequenceContainer;NINA.Sequencer.SequenceItem.ISequenceItem" OnDropCommand="DropIntoCommand" />
                                </i:Interaction.Behaviors>
                            </Grid>
                        </Border>
                    </Grid>
                </ninactrl:DetachingExpander>

            </nina:SequenceBlockView.SequenceItemContent>
        </nina:SequenceBlockView>
    </DataTemplate>

    <HierarchicalDataTemplate DataType="{x:Type local:IfConstant}" ItemsSource="{Binding Items}">
        <local:PowerupsContainerView>
            <local:PowerupsContainerView.HeaderAddon>
                <logic:ExprControl Margin="20,0,0,0" Label="Expression: " Exp="{Binding PredicateExpression}" />
            </local:PowerupsContainerView.HeaderAddon>
        </local:PowerupsContainerView>
    </HierarchicalDataTemplate>

    <HierarchicalDataTemplate DataType="{x:Type local:RepeatUntilAllSucceed}" ItemsSource="{Binding Items}">
        <local:PowerupsContainerView>
            <local:PowerupsContainerView.HeaderAddon>
                <StackPanel Orientation="Horizontal">
                    <logic:ExprControl Margin="0,2,0,0" Label="Wait " Exp="{Binding Path=WaitExpression, Mode=TwoWay}" />
                    <TextBlock VerticalAlignment="Center" Margin="0,2,0,0" Text=" s before repeating " />
                </StackPanel>
            </local:PowerupsContainerView.HeaderAddon>
        </local:PowerupsContainerView>
    </HierarchicalDataTemplate>

     <!-- OnceSafe: When-style expander for the whole block + drop into OnceSafe.Items (virtualized) -->
    <DataTemplate DataType="{x:Type local:OnceSafe}">
        <Border>
            <Border.Resources>
                <wpfutil:SharedResourceDictionary Source="/NINA.Sequencer;component/Resources/Styles/ProgressStyle.xaml" />
            </Border.Resources>

            <i:Interaction.Behaviors>
                <behaviors:DragOverBehavior AllowDragCenter="False" DragAboveSize="25" DragBelowSize="15" />
            </i:Interaction.Behaviors>

            <ninactrl:DetachingExpander IsExpanded="{Binding IsExpanded, Mode=TwoWay}">
                <ninactrl:DetachingExpander.Header>
                    <Grid
                        MinHeight="45"
                        Margin="0"
                        HorizontalAlignment="{Binding HorizontalAlignment, RelativeSource={RelativeSource AncestorType=ContentPresenter}, Mode=OneWayToSource}"
                        Background="{StaticResource SecondaryBackgroundBrush}">
                        <Grid.ColumnDefinitions>
                            <ColumnDefinition Width="Auto" />
                            <ColumnDefinition />
                            <ColumnDefinition Width="Auto" />
                        </Grid.ColumnDefinitions>

                        <StackPanel Orientation="Horizontal">
                          <TextBlock
                              MinHeight="25"
                              Margin="10,10,40,0"
                              Foreground="White"
                              HorizontalAlignment="Left"
                              VerticalAlignment="Center"
                              Text="{Binding Name}" />

                          <Border
                              Width="20"
                              Height="20"
                              Margin="5,0,5,0"
                              Background="{StaticResource NotificationErrorBrush}"
                              BorderBrush="Transparent"
                              CornerRadius="10">
                            <Border.Visibility>
                              <PriorityBinding>
                                <Binding Path="Issues.Count" Converter="{StaticResource ZeroToVisibilityConverter}" FallbackValue="Collapsed" />
                              </PriorityBinding>
                            </Border.Visibility>
                            <Path
                                HorizontalAlignment="Right"
                                Data="{StaticResource ExclamationCircledSVG}"
                                Fill="{StaticResource ButtonForegroundBrush}"
                                Stretch="Uniform" />
                            <Border.ToolTip>
                              <ItemsControl ItemsSource="{Binding Issues}" />
                            </Border.ToolTip>
                          </Border>
                        </StackPanel>

                        <Border Grid.Column="1" Background="{StaticResource SecondaryBackgroundBrush}">
                          <ContentPresenter
                              Margin="2,0,10,0"
                              HorizontalAlignment="Right"
                              Content="{Binding}"
                              DockPanel.Dock="Right"
                              Style="{StaticResource ProgressPresenter}" />
                        </Border>

                        <Border
                            x:Name="ButtonCommands"
                            Grid.Column="2"
                            HorizontalAlignment="Stretch"
                            Background="{StaticResource SecondaryBackgroundBrush}">
                          <Control Template="{StaticResource PowerupsButtons}" />
                        </Border>
                    </Grid>
                </ninactrl:DetachingExpander.Header>

                <!-- Body: drop directly into OnceSafe.Items -->
                <Border
                    MinHeight="35"
                    Margin="0,0,0,0"
                    BorderBrush="{StaticResource BorderBrush}"
                    BorderThickness="1">
                  <Grid Background="{StaticResource BackgroundBrush}">
                    <ListBox
                        MinHeight="35"
                        ItemsSource="{Binding Items}"
                        VirtualizingPanel.IsVirtualizing="True"
                        VirtualizingPanel.VirtualizationMode="Recycling"
                        VirtualizingPanel.ScrollUnit="Pixel"
                        ScrollViewer.HorizontalScrollBarVisibility="Disabled"
                        ScrollViewer.VerticalScrollBarVisibility="Auto"
                        Background="Transparent"
                        BorderThickness="0"
                        HorizontalContentAlignment="Stretch">
                      <ListBox.ItemContainerStyle>
                        <Style TargetType="ListBoxItem">
                          <Setter Property="Padding" Value="0" />
                          <Setter Property="Margin" Value="0" />
                          <Setter Property="HorizontalContentAlignment" Value="Stretch" />
                          <Setter Property="Template">
                            <Setter.Value>
                              <ControlTemplate TargetType="ListBoxItem">
                                <ContentPresenter />
                              </ControlTemplate>
                            </Setter.Value>
                          </Setter>
                        </Style>
                      </ListBox.ItemContainerStyle>
                    </ListBox>

                    <TextBlock
                        HorizontalAlignment="Left"
                        VerticalAlignment="Center"
                        Margin="30,0,0,0"
                        FontStyle="Italic"
                        Opacity="0.4"
                        Text="Instructions"
                        Visibility="{Binding Items.Count, Converter={StaticResource InverseZeroToVisibilityConverter}}" />

                    <i:Interaction.Behaviors>
                      <behaviors:DragOverBehavior DragAboveSize="0" DragBelowSize="0" />
                      <behaviors:DropIntoBehavior
                          AllowedDragDropTypesString="NINA.Sequencer.Container.ISequenceContainer;NINA.Sequencer.SequenceItem.ISequenceItem;NINA.Sequencer.TemplatedSequenceContainer"
                          OnDropCommand="DropIntoCommand" />
                    </i:Interaction.Behaviors>
                  </Grid>
                </Border>
            </ninactrl:DetachingExpander>
        </Border>
    </DataTemplate>
</ResourceDictionary>